# FortiHoney

Production-ready FortiGate honeypot. All code is in `fortihoney-flask/`.

## Quick Start

```bash
cd fortihoney-flask
./setup.sh
docker compose up -d
```

See `fortihoney-flask/README.md` for full documentation.
