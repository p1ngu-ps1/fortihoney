# FortiHoney Environment Configuration
# Copy this file to .env and set your values

# Environment (production or development)
GO_ENV=production

# API Key for secure log retrieval (REQUIRED for production!)
# Generate a secure key: python3 -c "import secrets; print(secrets.token_urlsafe(32))"
FORTIHONEY_API_KEY=your-secure-random-key-here
